/*Construct a program to check whether the number is greater than 70 or  not. If greater than 70, display ‘Pass’. If the number is 70 and below, display  ‘try again’.*/
#include <stdio.h>
int main()
{
    int mark;
    printf("Enter the mark :");
    scanf("%d", &mark);
    if(mark >70){
        printf("Pass");
    }
    if(mark <70){
        printf("Try Again");
    }
    return 0;
}
